I am building a codebase to supervise finetune an LLM (Llama-3 1B). 
I am responsible for building the supervised finetuning pipeline. 
Make sure the codebase is modular so that we can modify it later.